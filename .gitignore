# QNX build outputs
nto/
*.so
*.o
o.le/
so.le/

# Deploy staging (created by scripts/demo.sh)
scripts/stage/

# Python
__pycache__/
*.pyc

# Secrets / env
.env
*.env
# Your own QNX_IP / LAPTOP_IP, copied from scripts/env.example.sh. SETUP.md tells you to
# create this, so it has to be ignored here or the first commit publishes your addresses.
scripts/env.local.sh

# Editor / OS
*.swp
.DS_Store

# Compiled connectors (vss/connector/Makefile output)
vss/connector/aq_signal_publisher
vss/connector/aq_signal_connector
